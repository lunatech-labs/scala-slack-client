# test/resources/application.conf template
# change values between <>
test {
  token = "<Your bot token>"
  channel = "<Channel id to send the message>"
  userId = "<Id of the user who will receive the ephemeral message>"
  email = "<email from user>"
}